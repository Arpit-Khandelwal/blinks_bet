
import { createWriteStream } from 'fs';
import { join } from 'path';

export async function blurImage(image: string): Promise<string> {


  return new Promise((resolve, reject) => {
    setTimeout(() => {
      resolve("")
    }, 100);
  })
}